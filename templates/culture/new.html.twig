{% extends 'base.html.twig' %}

{% block title %}nouvelle Culture{% endblock %}
 
{% block body %}
<main style="flex;align-items: center;
justify-content: center;
 
          position: fixed;
           
            left: 500px;
            bottom: 250px;
            width: 40%;
             height: 390px;
            text-align: center;">
    <div class="row">
        

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Ajouter Culture </h5>

                    <!-- General Form Elements -->
                    
                   {{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
                   <p>
                        <label for="inputText" class="col-sm-4 col-form-label">type</label>
                        <div class="col-sm-20">
                            {{ form_widget(form.type,{'attr':{'class':'form-control'}}) }}

                        </div>
                    {{ form_errors(form.type,{'attr':{'class':'text-danger'}}) }}

</p>
<p>
                    <label for="inputText" class="col-sm-4 col-form-label">date_plantation</label>
                        <div class="col-sm-10">
                            {{ form_widget(form.date_planting,{'attr':{'class':'form-control'}}) }}

                    </div>
                    {{ form_errors(form.date_planting,{'attr':{'class':'text-danger'}}) }}

                  </p> 
 <p>
                   {{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
                        <label for="inputText" class="col-sm-4 col-form-label">quantité</label>
                        <div class="col-sm-10">
                            {{ form_widget(form.quantite,{'attr':{'class':'form-control'}}) }}

                        </div>
                    {{ form_errors(form.quantite,{'attr':{'class':'text-danger'}}) }}

</p>
                    {{ form_end(form) }}

               
            </div>

        </div>

    </div>


{% endblock %}